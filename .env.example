# For Vercel: enables writes to persist by committing data/journal.json to GitHub.
# If unset, the app falls back to local file writes (good for dev, ephemeral on Vercel).
GITHUB_TOKEN=
GITHUB_REPO=websitesbymartijn/cc-journal
GITHUB_BRANCH=main
